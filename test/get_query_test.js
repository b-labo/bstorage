/**
 * Test case for getQuery.
 * Runs with mocha.
 */
'use strict'

const getQuery = require('../lib/helpers/get_query.js')
const assert = require('assert')
const co = require('co')

describe('get-query', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Get query', () => co(function * () {

  }))
})

/* global describe, before, after, it */
