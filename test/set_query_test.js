/**
 * Test case for setQuery.
 * Runs with mocha.
 */
'use strict'

const setQuery = require('../lib/helpers/set_query.js')
const assert = require('assert')


describe('set-query', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Set query', async () => {

  })
})

/* global describe, before, after, it */
