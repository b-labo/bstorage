/**
 * Test case for setQuery.
 * Runs with mocha.
 */
'use strict'

const setQuery = require('../lib/helpers/set_query.js')
const assert = require('assert')
const co = require('co')

describe('set-query', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Set query', () => co(function * () {

  }))
})

/* global describe, before, after, it */
